# llm-sym

Interaktywna symulacja incydentu Hugging Face (METR, sierpień 2026) — pojedyncza strona `index.html`.

## Budowanie

```bash
python3 build.py
```

Skrypt składa `index.html` z modułów: `data_events.py` (dane), `styles.py` (CSS), `templates.py` (HTML), `scripts_js.py` (JS).
